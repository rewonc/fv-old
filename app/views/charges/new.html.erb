<div class="row">
  <div class="small-12 columns">
    
    <h2 class="header">Last step! Confirm and pay</h2>

    
    
    <div class="row panel-box">
      <div class="small-7 small-centered columns">

        <div class="row">
          <div class="small-12 columns">
            <h4 class="subheader">Your order details</h4>
            <p>You selected a <strong><%= @box.get_box_name %></strong>.</p>
            <p>Your total price will be <strong>$<%= @box.get_price.to_s.chop.chop + '.' + @box.get_price.to_s.last(2) %>,</strong> shipping included, per week.</p>
               <p>Your first delivery will be on <strong><%= @box.startdate %></strong>. Your preferred time of delivery is: <strong><%= @box.delivery_preference_string %></strong></p>
      
          </div>
        </div>
        <div class="row">
          <div class="small-6 columns">
              <p><strong>Delivery address:</strong><br/>
                <%= @box.firstname + ' ' + @box.lastname%><br />
                <%= @box.street%><br />
                <% if @box.street2.present? %>
                <%= @box.street2 %><br />
                <% end %>
                <%= @box.city %>, <%= @box.state %> <%= @box.zip %><br />
              </p>
          </div>
          <div class="small-6 columns">
              <p><strong>Contact information:</strong><br/>
                <%= @box.email %><br />
                <%= @box.phone %><br />
              </p>
          </div>
        </div>

        <div class="row">
          <div class="small-4 columns small-centered centered" id="payment_box">
            
            <%= form_tag charges_path do %>
             
              <script src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button"
                      data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                      data-description="For delivery <%= @box.startdate%>"
                      data-amount=<%= @box.get_price %> ></script>
            <% end %>
            
          </div>
        </div>


        <div class="row">
          <div class="small-12 columns centered"><%= link_to "Modify your order", edit_box_path(session[:box_id]) %></div>
        </div>

      </div>
    </div>

  </div>
</div>



<div class="row">
      <h3>Our policy</h3>
      <ul class="square">
        <li><p>You will be charged immediately for your first box</p></li>
        <li><p>You can skip a week or cancel your subscription at anytime by emailing us at <%= mail_to 'hello@farmivore.com', "hello@farmivore.com" %>, at least 4 days in advance of your order.</p></li>
        <li><p>Satisfaction guaranteed. If you don't like it, you don't pay.</p></li>
      </ul>
</div>

