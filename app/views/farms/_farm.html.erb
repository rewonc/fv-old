 <tr>
      <td><%= image_tag farm.logo.url('thumb') %></td>
      <td><%= link_to farm.title, farm_path(farm.id) %></td>
      <td><%= farm.address %></td>
      <td><%= farm.description %></td>
    </tr>