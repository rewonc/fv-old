<tr>
        <td><%= line_item.product.title %></td>
        <td><%= line_item.product.farm.title %></td>
        <td><%= line_item.quantity %></td>
        <td><%= line_item.product.price %></td>
        <td><%= link_to 'Edit Quantity', edit_line_item_path(line_item) %></td>
        <td><%= link_to 'Remove from cart', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>